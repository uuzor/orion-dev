[build]
  base = "server"
  command = "npm install && npm run build"
  publish = "dist"
  functions = "netlify/functions"
  environment = { SECRETS_SCAN_OMIT_PATHS = "server/**,*.md,src/whatsapp/**" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
