[build]
  base = ""
  publish = "dist"
  functions = "netlify/functions"
  environment = { SECRETS_SCAN_OMIT_PATHS = "server/**,*.md" }

# Note: dist is pre-built, _redirects copied to dist folder for API redirects
